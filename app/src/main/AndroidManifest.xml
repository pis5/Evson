<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mkass.evson">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_balloons"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDlZdiIGyStXXZDwok42_b3NHPI8HDywQo"/>

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signin" />
        <activity android:name=".signup" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NewEventActivity"
            android:label="@string/title_activity_new_event"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".FriendsActivity"
            android:label="@string/title_activity_friends"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".AddFriendsActivity"
            android:label="@string/title_activity_friends"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".InvitesFriendsActivity"
            android:label="@string/title_activity_friends"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".MyProfileActivity"
            android:label="@string/title_activity_friends"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".LookForFriendActivity"
            android:label="@string/title_activity_look_for_friend"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>